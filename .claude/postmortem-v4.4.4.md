# Постмортем: Деплой v4.4.4
## Дата: 2025-11-07

---

## ❌ ФАКАП #1: Не розібрався з архітектурою деплою на старті

### Що сталося:
Припустив що GitHub Pages деплоїться з гілки `master` (стандартна практика), але насправді деплой був налаштований на feature гілку `claude/digital-tarot-prototype-011CUrUqUHgzsoJVYczpnSUM`.

### Чому це сталося:
- Не запитав користувача про налаштування GitHub Pages
- Не перевірив `.github/workflows/` на наявність кастомних CI/CD
- Діяв за стандартним шаблоном без валідації

### Втрачений час: ~15 хвилин

### Як запобігти:
✅ **ПЕРШЕ ЩО РОБИТИ:** Запитати користувача "З якої гілки деплоїться проект?"
✅ Перевірити наявність `.github/workflows/*.yml` для кастомних деплоїв
✅ Глянути README.md на наявність інструкцій з деплою
✅ НЕ припускати стандартну конфігурацію

**ПРАВИЛО:** Завжди валідуй архітектуру перед початком роботи.

---

## ❌ ФАКАП #2: Жорстко прописана версія в vite.config.js

### Що сталося:
У файлі `vite.config.js` (рядки 12-14) була жорстко прописана версія `v4.3` в назвах файлів:
```javascript
entryFileNames: `assets/[name]-[hash]-v4.3.js`
```

Навіть після оновлення `package.json` до `4.4.4`, build генерував файли з `-v4.3.js`.

### Чому це сталося:
- Не перевірив vite.config.js перед оновленням версії
- Сфокусувався тільки на package.json
- Не знав що версія може бути в конфігурації білда

### Втрачений час: ~10 хвилин

### Як запобігти:
✅ **При оновленні версії ЗАВЖДИ grep всі конфіги:**
```bash
grep -r "4.3\|v4\.3" . --include="*.js" --include="*.json" --include="*.config.*"
```
✅ Перевірити конфіги білд-систем: vite.config.js, webpack.config.js, rollup.config.js
✅ Версія має бути в ОДНОМУ місці - package.json, інші файли мають читати звідти

**ПРАВИЛО:** Версія = single source of truth (package.json). Ніколи не дублювати вручну.

---

## ❌ ФАКАП #3: Забув оновити APP_VERSION в компоненті

### Що сталося:
В `src/TarotReader.jsx:5` була константа:
```javascript
const APP_VERSION = '4.3';
```

Оновив package.json та vite.config, але забув про цю константу в коді, яка відображається в UI.

### Чому це сталося:
- Не зробив повний grep версії в src/
- Думав що версія тільки в конфігах
- Не подумав що версія може показуватись користувачу в UI

### Втрачений час: ~5 хвилин

### Як запобігти:
✅ **При оновленні версії grep ВСІ файли:**
```bash
grep -r "VERSION\|version.*=\|4\.3\|v4\.3" src/ --include="*.jsx" --include="*.js" --include="*.ts" --include="*.tsx"
```
✅ Шукати константи типу `APP_VERSION`, `VERSION`, `CURRENT_VERSION`
✅ В React/Vue компонентах версія має імпортуватись з package.json:
```javascript
import { version } from '../package.json';
const APP_VERSION = version;
```

**ПРАВИЛО:** Версія має бути в package.json і імпортуватись звідти. Ніяких жорстко прописаних констант.

---

## ❌ ФАКАП #4: Не додав запис v4.4.4 в CHANGELOG масив компонента

### Що сталося:
Створив `CHANGELOG.md` файл, оновив версії в конфігах, але забув додати новий запис у масив `CHANGELOG` всередині компонента (рядок 7-24), який відображається в UI модального вікна.

### Чому це сталося:
- Не перевірив де ще показується changelog в додатку
- Припустив що CHANGELOG.md достатньо
- Не подумав про UI компоненти

### Втрачений час: ~7 хвилин

### Як запобігти:
✅ **При оновленні changelog grep де він використовується:**
```bash
grep -r "CHANGELOG\|changelog" src/ --include="*.jsx" --include="*.js"
```
✅ Перевірити чи є масиви/об'єкти з історією версій в компонентах
✅ КРАЩЕ: Мати один джерело (CHANGELOG.md) і парсити його для UI

**ПРАВИЛО:** Changelog = single source of truth. Якщо є дублювання в коді - рефакторити.

---

## ❌ ФАКАП #5: Спроба мерджити в protected master

### Що сталося:
Витратив час на спробу `git push origin master`, отримав 403 error, намагався створити PR через `gh pr create` (також заблоковано).

### Чому це сталося:
- Не перевірив що master - protected branch
- Не усвідомив що деплой НЕ з master (див. Факап #1)
- Діяв механічно без розуміння архітектури

### Втрачений час: ~10 хвилин

### Як запобігти:
✅ **Спочатку дізнатись про архітектуру деплою** (див. Факап #1)
✅ Перевірити branch protection rules перед push:
```bash
git push --dry-run origin master  # тестовий push
```
✅ Якщо деплой з feature гілки - пушити ТУДИ, не мерджити нікуди

**ПРАВИЛО:** Розумій де деплой, пуш туди. Не мерджити "на всяк випадок".

---

## ❌ ФАКАП #6: Браузерний кеш - не зробив cache bust одразу

### Що сталося:
Після кількох білдів і пушів користувач все одно бачив стару версію. Проблема була в браузерному кеші - старі файли `index-3e29a3e4.js` були закешовані.

### Чому це сталося:
- Не подумав про browser caching
- Vite генерує хеші, але якщо код не міняється - хеш той самий
- Не зробив "порожню зміну" для форсування нового хешу

### Втрачений час: ~8 хвилин (+ фрустрація користувача)

### Як запобігти:
✅ **При проблемах з деплоєм ЗАВЖДИ думай про кеш**
✅ Якщо треба форсувати новий білд - додати timestamp/коментар:
```bash
echo "/* v4.4.4 - $(date +%s) */" >> src/main.jsx
npm run build  # це згенерує НОВИЙ хеш
```
✅ Видалити старі файли перед білдом:
```bash
rm -rf dist/ assets/*.js assets/*.css
npm run build
```
✅ Перевірити що index.html посилається на правильні файли після білду

**ПРАВИЛО:** При деплої завжди думай: "Чи зламає це кеш браузера?". Якщо ні - зламай вручну.

---

## ❌ ФАКАП #7: index.html містив жорстко прописані шляхи

### Що сталося:
Після першого білду `index.html` мав жорстко прописані шляхи:
```html
<script src="./assets/index-3e29a3e4.js"></script>
```

Це НЕ Vite entry point, тому наступні білди не оновлювали ці шляхи.

### Чому це сталося:
- postbuild скрипт копіює dist/index.html в root, перезаписуючи правильний entry point
- Не перевірив що index.html - це Vite template, не build artifact
- Плутанина між source index.html і build index.html

### Втрачений час: ~12 хвилин

### Як запобігти:
✅ **index.html має бути Vite entry point:**
```html
<script type="module" src="/src/main.jsx"></script>
```
✅ Vite SAM створює dist/index.html з правильними хешованими шляхами
✅ postbuild скрипт має копіювати саме dist/index.html (що Vite згенерував)
✅ НЕ редагувати root index.html вручну після білду

**ПРАВИЛО:** Source index.html = Vite template. Build index.html = Vite output. Не плутати їх.

---

## ❌ ФАКАП #8: Не використав TodoWrite проактивно

### Що сталося:
Користувач розгублений що відбувається, бо я не планував кроки публічно через TodoWrite.

### Чому це сталося:
- Забув використати TodoWrite на початку
- Не розбив проблему на чіткі кроки
- Користувач не бачив прогресу

### Втрачений час: N/A (але фрустрація користувача)

### Як запобігти:
✅ **ЗАВЖДИ починати з TodoWrite** коли є багатокрокова задача
✅ Розбити задачу на атомарні кроки
✅ Оновлювати статус після кожного кроку
✅ Це дає користувачу впевненість що ти знаєш що робиш

**ПРАВИЛО:** Складна задача = обов'язково TodoWrite. Користувач має бачити план.

---

## 📊 СТАТИСТИКА ФАКАПІВ

| Факап | Втрачений час | Серйозність |
|-------|---------------|-------------|
| #1 Архітектура деплою | 15 хв | 🔴 Висока |
| #2 vite.config.js версія | 10 хв | 🟡 Середня |
| #3 APP_VERSION константа | 5 хв | 🟡 Середня |
| #4 CHANGELOG масив | 7 хв | 🟢 Низька |
| #5 Protected master | 10 хв | 🟡 Середня |
| #6 Браузерний кеш | 8 хв | 🟡 Середня |
| #7 index.html entry point | 12 хв | 🔴 Висока |
| #8 TodoWrite | N/A | 🟢 Низька |
| **ВСЬОГО** | **~67 хв** | |

---

## ✅ ГОЛОВНІ ВИСНОВКИ

### 1. **ЗАВЖДИ починати з дослідження архітектури**
- Яка структура проекту?
- Звідки деплой? (гілка, CI/CD)
- Які конфіги білд-системи?
- Де зберігається версія?

### 2. **Версія = Single Source of Truth**
- Версія ТІЛЬКИ в package.json
- Всі інші місця мають імпортувати звідти
- При зміні версії - grep ВСЕ

### 3. **Vite/Build system patterns**
- index.html = Vite template з `/src/main.jsx` entry point
- dist/index.html = згенерований Vite з хешованими шляхами
- postbuild копіює dist/* в root для GitHub Pages
- НЕ редагувати build artifacts вручну

### 4. **Браузерний кеш - твій ворог**
- При проблемах з деплоєм - завжди думай про кеш
- Хеші файлів = автоматичний cache bust
- Якщо треба форсувати - додати timestamp в код

### 5. **Git workflow при protected branches**
- Дізнатись про protection rules перед роботою
- Якщо деплой з feature гілки - пушити ТУДИ
- Не намагатись мерджити в master якщо не треба

### 6. **Комунікація з користувачем**
- TodoWrite для багатокрокових задач
- Чіткий план = менше фрустрації
- Оновлювати прогрес в реальному часі

---

## 🎯 ACTION ITEMS для майбутніх деплоїв

**Pre-flight checklist (ПЕРЕД будь-якою зміною версії):**

```bash
# 1. Дізнатись про деплой
echo "З якої гілки деплой?" # Запитати користувача

# 2. Знайти ВСІ місця з версією
grep -r "version\|VERSION" . --include="*.json" --include="*.js" --include="*.jsx" --include="*.config.*"

# 3. Знайти де показується версія користувачу
grep -r "VERSION\|CHANGELOG" src/ -A 5

# 4. Перевірити білд конфіги
cat vite.config.js webpack.config.js rollup.config.js 2>/dev/null

# 5. Створити план в TodoWrite
# - Оновити версію в X місцях
# - Додати changelog запис в Y місцях
# - Білд
# - Пуш на правильну гілку
```

**Build & Deploy checklist:**

```bash
# 1. Очистити старі файли
rm -rf dist/ assets/*.js assets/*.css

# 2. Білд
npm run build

# 3. Перевірити згенеровані файли
ls -lh dist/assets/
grep -E "src=|href=" dist/index.html

# 4. Перевірити що postbuild скопіював правильно
ls -lh assets/
grep -E "src=|href=" index.html

# 5. Commit з описовим повідомленням
git add -A
git commit -m "v4.4.4 - опис змін"

# 6. Push на ПРАВИЛЬНУ гілку (ту що деплоїться)
git push -u origin <DEPLOY_BRANCH>
```

---

## 💡 Ідеї для покращення проекту

1. **Автоматичний version sync**
   - Script що читає версію з package.json і оновлює всі константи
   - Pre-build hook що валідує версії в різних місцях

2. **Кращий changelog management**
   - Парсити CHANGELOG.md для UI замість дублювання в коді
   - Використати бібліотеку типу `conventional-changelog`

3. **Cache busting strategy**
   - Завжди використовувати content hashes (що Vite робить)
   - Додати cache headers для правильного caching

4. **Deploy automation**
   - GitHub Actions для автоматичного деплою з конкретної гілки
   - Автоматичні тести перед деплоєм

---

**Підпис:** Claude (Sonnet 4.5)
**Дата:** 2025-11-07
**Статус:** Все працює, користувач щасливий ✅
