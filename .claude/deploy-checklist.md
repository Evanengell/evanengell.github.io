# 🚀 Deploy Checklist - Швидкий гайд

## ⚡ BEFORE STARTING (5 хвилин дослідження)

### 1. Дізнатись про архітектуру
- [ ] **ЗАПИТАТИ:** "З якої гілки деплоїться проект?"
- [ ] Перевірити `.github/workflows/*.yml`
- [ ] Прочитати README.md секцію про деплой
- [ ] Перевірити чи є protected branches

### 2. Знайти де версія зберігається
```bash
grep -r "version\|VERSION\|v4\." . \
  --include="*.json" \
  --include="*.js" \
  --include="*.jsx" \
  --include="*.config.*" \
  --exclude-dir=node_modules \
  --exclude-dir=dist
```

### 3. Створити TodoWrite з планом
```
- Оновити версію в package.json
- Оновити версію в vite.config.js (якщо є)
- Оновити APP_VERSION константу в src/
- Додати changelog запис в код
- Білд
- Пуш на DEPLOY гілку
```

---

## 📝 ОНОВЛЕННЯ ВЕРСІЇ

### 1. package.json
```bash
# Знайти поточну версію
grep '"version"' package.json

# Оновити (або через Edit tool)
```

### 2. vite.config.js / webpack.config.js
```bash
# Перевірити чи є версія в конфігах білду
cat vite.config.js | grep -i version

# Якщо є - видалити! Версія має бути тільки в package.json
# АБО використовувати динамічно:
# entryFileNames: `assets/[name]-[hash].js`  # БЕЗ версії!
```

### 3. src/ компоненти
```bash
# Знайти APP_VERSION або подібні константи
grep -r "VERSION\|version.*=" src/ --include="*.jsx" --include="*.js"

# Оновити через Edit tool
```

### 4. CHANGELOG
```bash
# Перевірити чи є CHANGELOG масив в компонентах
grep -r "CHANGELOG" src/ -A 3

# Додати новий запис на початок масиву
```

---

## 🔨 BUILD PROCESS

### 1. Очистити старі файли (для cache bust)
```bash
rm -rf dist/ assets/*.js assets/*.css
```

### 2. Білд
```bash
npm run build
```

### 3. Перевірити результат
```bash
# Перевірити згенеровані файли
ls -lh dist/assets/

# Перевірити dist/index.html
grep -E "src=|href=" dist/index.html | grep -E "\.js|\.css"

# Перевірити що postbuild скопіював в root
ls -lh assets/
grep -E "src=|href=" index.html | grep -E "\.js|\.css"
```

**ВАЖЛИВО:**
- `dist/index.html` має посилатись на хешовані файли
- `index.html` (root) теж має посилатись на ТІ САМІ хешовані файли

---

## 🚀 DEPLOY

### 1. Commit
```bash
git add -A
git commit -m "v4.X.X - Короткий опис змін

Детальніше:
- Що додано
- Що виправлено
- Що покращено
"
```

### 2. Push на ПРАВИЛЬНУ гілку
```bash
# ВАЖЛИВО: Пушити на ту гілку, з якої деплоїться!
# НЕ на master якщо деплой з feature гілки!

git push -u origin <DEPLOY_BRANCH_NAME>
```

### 3. Перевірити деплой через 1-2 хв
- Відкрити сайт
- Hard refresh: Ctrl+Shift+R (Windows) / Cmd+Shift+R (Mac)
- Перевірити версію в футері
- Перевірити changelog

---

## 🐛 ЯКЩО ЩОСЬ НЕ ТАК

### Проблема: Стара версія на сайті після деплою

**Причина 1: Браузерний кеш**
```bash
# Додати timestamp для нового хешу
echo "/* Cache bust $(date +%s) */" >> src/main.jsx

# Перебілдити
rm -rf dist/ assets/*.js
npm run build

# Запушити
git add -A && git commit -m "Cache bust" && git push
```

**Причина 2: Пушнули не на ту гілку**
```bash
# Перевірити з якої гілки деплой (запитати користувача!)
# Пушнути на правильну гілку
git push -u origin <CORRECT_DEPLOY_BRANCH>
```

**Причина 3: index.html не має правильних шляхів**
```bash
# index.html має бути Vite template:
<script type="module" src="/src/main.jsx"></script>

# НЕ жорстко прописані шляхи!
# Vite сам згенерує правильні шляхи в dist/index.html
```

---

## ✅ SUCCESS CRITERIA

Деплой вважається успішним коли:

1. [ ] Новий JS файл з унікальним хешем згенеровано
2. [ ] index.html посилається на новий файл
3. [ ] Зміни запушені на DEPLOY гілку
4. [ ] Після hard refresh сайт показує нову версію
5. [ ] Footer показує правильну версію (напр. v4.4.4)
6. [ ] Changelog містить новий запис
7. [ ] Всі нові фічі працюють як очікувалось

---

## 🎓 MAIN LESSONS

1. **Дізнайся про архітектуру ПЕРШ НІЖ щось робити**
2. **Версія = single source of truth (package.json)**
3. **Vite entry point: `/src/main.jsx` в source index.html**
4. **Cache bust = нові хеші файлів**
5. **Деплой на ту гілку, з якої реально деплоїться сайт**

---

**Версія чеклісту:** 1.0
**Останнє оновлення:** 2025-11-07
**Статус:** Протестовано на v4.4.4 ✅
